'use client'
import { FormEvent, useState } from 'react'
import { ArrowRight, Mail, Sparkles } from 'lucide-react'
import { useRouter } from 'next/navigation'
export default function Login(){const [email,setEmail]=useState('');const router=useRouter();const submit=(e:FormEvent)=>{e.preventDefault();if(email.trim())router.push('/onboarding')};return <main className="min-h-screen bg-[#070712] text-white grid place-items-center px-6 py-12"><div className="w-full max-w-md"><a href="/" className="mx-auto mb-10 flex w-fit items-center gap-2 text-xl font-black"><span className="grid h-9 w-9 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-fuchsia-500">V</span>ViralPilot</a><section className="rounded-[30px] border border-white/10 bg-white/[.06] p-7 shadow-2xl backdrop-blur-xl md:p-9"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-violet-500/15 text-violet-300"><Sparkles size={21}/></div><h1 className="mt-6 text-3xl font-black tracking-tight">Start your growth workspace.</h1><p className="mt-3 text-sm leading-6 text-white/50">Create a lightweight account to save analyses and build your content plan.</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-sm font-semibold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="mt-2 w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3.5 outline-none placeholder:text-white/25 focus:border-violet-400"/></label><button className="w-full rounded-xl bg-white py-3.5 font-bold text-black">Continue <ArrowRight className="ml-1 inline" size={16}/></button></form><p className="mt-5 flex items-center justify-center gap-2 text-center text-xs text-white/35"><Mail size={13}/> Production auth will use a secure provider; no Instagram password is requested.</p></section></div></main>}
